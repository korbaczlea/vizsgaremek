# Loaded inside `http { }` before `default.conf`. Without this, nginx defaults to
# client_max_body_size 1m → 413 Payload Too Large on gallery uploads.
client_max_body_size 512m;
client_body_timeout 600s;
